# -----------------------------------------------------------------------------
# alias.nu
# Conservative aliases. Avoid overriding Nu builtins by default.
# -----------------------------------------------------------------------------

alias q = exit
alias c = clear

alias e = edit
alias v = nvim

alias gs = ^git status
alias gl = ^git log --oneline --decorate -n 20
